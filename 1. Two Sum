//First submission wrong!! Should check the constraints that the array can have negative elements also.
//Constraints:
//2 <= nums.length <= 10^4
//-10^9 <= nums[i] <= 10^9
//-10^9 <= target <= 10^9

//Wrong at test case
//Input
//nums = [-1,-2,-3,-4,-5]
//target = -8
//Output = [-1,-1]
//Expected Output = [2,4]

class Solution {
public:
    vector<int> twoSum(vector<int>& nums, int target) {
        unordered_map<int,int>mp;
        for(int i=0;i<nums.size();i++){
            mp[nums[i]]=i;
        }
        for(int i=0;i<nums.size();i++){
            if(mp.find(abs(target-nums[i]))!=mp.end()&&mp[abs(target-nums[i])]!=i){
                return {i,mp[target-nums[i]]};
            }
        }
        return {-1,-1};
    }
};



//Correct submission

class Solution {
public:
    vector<int> twoSum(vector<int>& nums, int target) {
        unordered_map<int,int>mp;
        for(int i=0;i<nums.size();i++){
            mp[nums[i]]=i;
        }
        for(int i=0;i<nums.size();i++){
            if(mp.find(target-nums[i])!=mp.end()&&mp[target-nums[i]]!=i){
                return {i,mp[target-nums[i]]};
            }
        }
        return {-1,-1};
    }
};
