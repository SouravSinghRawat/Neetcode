class Solution {
public:
    void helper(vector<int> &nums,int ind,vector<int> &temp,int n,vector<vector<int>> &ans){
        ans.push_back(temp);
        for(int i=ind;i<n;i++){
            if(i!=ind&&nums[i]==nums[i-1]) continue;
            temp.push_back(nums[i]);
            helper(nums,i+1,temp,n,ans);
            temp.pop_back();
        }
    }
    vector<vector<int>> subsetsWithDup(vector<int>& nums) {
        int n=nums.size();
        vector<int>temp;
        sort(nums.begin(),nums.end());
        vector<vector<int>> ans;
        helper(nums,0,temp,n,ans);
        return ans;

    }
};

//Tip: Ignore the duplicates.
